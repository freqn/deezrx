<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Welcome to DeezRX</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <nav class="main-nav">
      <a class="main-nav__logo" href="/">Deez<span style="color:orange">Rx</span></a>
      <ul class="main-nav__links">
        <%= if @current_user do %>
          <li><%= @current_user.email %></li>
          <li><%= link "log out", to: session_path(@conn, :delete) %></li>
        <% else %>
          <li><%= link "sign in", to: session_path(@conn, :new) %></li>
        <% end %>
      </ul>
    </nav>

    <main class="content">
      <%= if message=get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= message %></p>
      <% end %>

      <%= if message=get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= message %></p>
      <% end %>

      <%= render @view_module, @view_template, assigns %>
    </main>

    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>